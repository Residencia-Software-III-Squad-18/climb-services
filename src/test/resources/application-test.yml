spring:
  datasource:
    url: jdbc:h2:mem:climb_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
app:
  contract-notifications:
    expiration-cron: "-"

google:
  calendar:
    client-id:
    client-secret:
    redirect-uri:
    frontend-url: http://localhost:5173
