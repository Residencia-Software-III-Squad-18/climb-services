spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:}
    username: ${SPRING_DATASOURCE_USERNAME:}
    password: ${SPRING_DATASOURCE_PASSWORD:}

  # Flyway excluído da app - migrations rodam apenas no container dedicado no deploy
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true